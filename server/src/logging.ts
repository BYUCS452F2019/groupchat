export const GET = (text: string) => {
  console.log(`GET ${text}`);
}

export const POST = (text: string) => {
  console.log(`POST ${text}`);
}

export const DELETE = (text: string) => {
  console.log(`DELETE ${text}`);
}